machine:
  python:
    version: 3.5.0

test:
    override:
        - py.test

dependencies:
    pre:
        - pip install -r requirements.txt

deployment:
    staging:
        - python setup.py sdist
        - python setup.py bdist_wheel